<Window x:Class="OutlastTrialsMod.Views.TextureModifyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:OutlastTrialsMod.Localization"
        WindowStyle="None"
        Background="#1a1c23"
        Foreground="#e0e0e0"
        ResizeMode="CanResize"
        MinWidth="720"
        MinHeight="650"
        Width="900"
        Height="650"
        WindowStartupLocation="CenterScreen">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="0" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#2b2d35" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#32343d" />
        <SolidColorBrush x:Key="AccentBrush" Color="#3a3d45" />

        <Style x:Key="PreviewPanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushDark}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="ClipToBounds" Value="True" />
        </Style>

        <Style x:Key="PreviewImageStyle" TargetType="Image">
            <Setter Property="Stretch" Value="Uniform" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushDark}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#454852" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Background="#15171d"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TitleText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Foreground="#e0e0e0"
                       FontSize="12"
                       TextTrimming="CharacterEllipsis"
                       WindowChrome.IsHitTestVisibleInChrome="True" />
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource ChromeButtonStyle}"
                        Content="&#x2014;"
                        Click="MinimizeButton_Click"
                        ToolTip="Minimize" />
                <Button Style="{StaticResource ChromeButtonStyle}"
                        Content="&#x25A1;"
                        Click="MaximizeButton_Click"
                        ToolTip="Maximize" />
                <Button Style="{StaticResource ChromeCloseButtonStyle}"
                        Content="&#x2715;"
                        Click="CloseButton_Click"
                        ToolTip="Close" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Text="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Original}"
                       FontWeight="SemiBold"
                       FontSize="13"
                       Margin="0,0,0,8"
                       Foreground="#e0e0e0" />
            <TextBlock Grid.Row="0"
                       Grid.Column="2"
                       Text="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=NewModification}"
                       FontWeight="SemiBold"
                       FontSize="13"
                       Margin="0,0,0,8"
                       Foreground="#e0e0e0" />

            <Border Grid.Row="1"
                    Grid.Column="0"
                    Style="{StaticResource PreviewPanelBorderStyle}">
                <Image x:Name="OriginalImage"
                       Style="{StaticResource PreviewImageStyle}" />
            </Border>

            <Border Grid.Row="1"
                    Grid.Column="2"
                    Style="{StaticResource PreviewPanelBorderStyle}">
                <Grid>
                    <Rectangle x:Name="PlaceholderRectangle"
                               Stroke="#6b6e78"
                               StrokeThickness="2"
                               StrokeDashArray="6 3"
                               RadiusX="4"
                               RadiusY="4"
                               Fill="Transparent"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch" />
                    <Image x:Name="ModifiedImage"
                           Style="{StaticResource PreviewImageStyle}"
                           Visibility="Collapsed" />
                </Grid>
            </Border>

            <Button Grid.Row="2"
                    Grid.Column="2"
                    Content="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Load}"
                    Style="{StaticResource ActionButtonStyle}"
                    HorizontalAlignment="Center"
                    Margin="0,12,0,0"
                    Click="LoadButton_Click" />
        </Grid>

        <Border Grid.Row="2"
                Background="#15171d"
                BorderBrush="{StaticResource BorderBrushDark}"
                BorderThickness="0,1,0,0"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="OriginalPathText"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0"
                           FontSize="10"
                           Foreground="#9a9a9a"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="Wrap" />
                <Button Grid.Column="1"
                        Content="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Done}"
                        Style="{StaticResource ActionButtonStyle}"
                        MinWidth="140"
                        Click="DoneButton_Click" />
            </Grid>
        </Border>
    </Grid>
</Window>
